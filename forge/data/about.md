(remove me once we have consensus on the folder name)

This folder contains specific implementations of common, re-used components, i.e. a `ChatEnv`.

For now this hosts only the `ToyEnv` for testing purposes.
